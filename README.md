# Notas:
Primer programa en Node

```
Options:
      --help     Show help                                   [boolean]
      --version  Show version number                         [boolean]
  -b, --base     Es la base de la tabla de multiplicar
                                                   [number] [required]
  -l, --listar   Muesta la tabla en consola [boolean] [default: false]
  -h, --hasta    Indica por cuantos numeros desea multiplicar un
                 numero                         [number] [default: 10]
```